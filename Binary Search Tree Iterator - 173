/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class BSTIterator { 
    //Optimal TC = O(H) SC = O(H)
    Stack<TreeNode> st = new Stack<TreeNode>();

    public BSTIterator(TreeNode root) {
       pushAllLeft(root);
    }
    
    private void pushAllLeft(TreeNode node) {
		while(node!=null) {
			st.push(node);
			node = node.left;
		}
		
	}
	public int next() {
        TreeNode node = st.pop();
        if(node.right != null)pushAllLeft(node.right);
        return node.val;
    }
    
    public boolean hasNext() {
        return !st.isEmpty();
    }
    //Brute TC = O(N) SC = O(N)+O(H)
   /*List<Integer> list;
    int index;
    public BSTIterator(TreeNode root) {
        list = new LinkedList<Integer>();//sc = o(n)
        index = 0;
        inOrder(root);
    }    
    private void inOrder(TreeNode root) { //TC- O(N) SC = O(H) [recursion stack]
		if(root == null)return;
		inOrder(root.left);
		list.add(root.val);
		inOrder(root.right);
		
	}
	public int next() {
        return list.get(index++);
    }
    
    public boolean hasNext() {
        return index<list.size();
    }*/
}

