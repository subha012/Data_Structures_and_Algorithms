class Solution {
    public int findKthPositive(int[] arr, int k) {
        //OPtimal TC = O(N) SC = O(1)
        for(int i = 0;i<arr.length;i++) {
        	if(arr[i]<=k)k++;
        	else break;
        }
		return k;

        //Brute TC = O(N+K) SC = O(1)
        /*int missingCount = 0;
        int num = 1;
        int i = 0;

        while (true) {
            if (i < arr.length && arr[i] == num) {//N
                i++; // number present
            } else {//K
                missingCount++;
                if (missingCount == k) {
                    return num;
                }
            }
            num++;
        }*/
    }
}
