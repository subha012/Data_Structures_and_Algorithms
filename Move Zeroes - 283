class Solution {
    public void moveZeroes(int[] nums) {

        int j =0;
		for(int i =0;i<nums.length;i++) {
			if(nums[i] != 0) {
				int temp = nums[i];
				nums[i] = nums[j];
				nums[j] = temp;
				j++; 
			}
		}
        //OPtimal(two pointer) TC = O(n=k) = O(n) SC =O(1)
        /*int i = 0;
        for(int j=0;j<nums.length;j++){//O(n)
            if(nums[j]!=0){
                nums[i] = nums[j];
                i++;
            }
        }
        for(int j =i;j<nums.length;j++){//O(k)
            nums[j] =0;
        }*/
        //Brute force Tc = O(n)+O(n)+O(n-temp.length) SC = O(n)
        /*int temp[] = new int[nums.length];
        int j =0;
        for(int i =0;i<nums.length;i++) { // TC - O(n)
        	if(nums[i]!= 0) {
        		temp[j] = nums[i];
        		j++;
        	}
        }
        for(int i =0;i<temp.length;i++) {//TC = O(N)
        	nums[i] = temp[i];
        }
        for(int i =temp.length;i<nums.length;i++) {//O(n- temp.length) 
        	nums[i] = 0;
        }*/
    }
}
