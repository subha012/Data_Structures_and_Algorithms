/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        //Optimal TC = O(N+M) SC = O(1) excluding answer array
        ListNode dummy = new ListNode(-1); //SC = O(N+M)
	    ListNode temp = dummy;
	    while(list1!=null && list2 != null) { //TC = O(N+M)
	    	if(list1.val<=list2.val) {
	    		temp.next = list1;
	    		list1= list1.next;
	    	}else {
	    		temp.next = list2;
	    		list2= list2.next;
	    	}
	    	temp = temp.next;
	    }
	    if(list1 != null) {
	    	temp.next = list1;
	    }else {
	    	temp.next = list2;
	    }
	    return dummy.next;
    }
        //Brute
        /*List<Integer> list = new ArrayList<>();// SC = O(N+M)
        while(list1 != null) { // TC = O(N)
            list.add(list1.val);
            list1 = list1.next;
        }
        while(list2 != null) {// TC = O(M)
            list.add(list2.val);
            list2 = list2.next;
        }
        Collections.sort(list);// TC = O((n+m)log(n+m))
        ListNode dummy = new ListNode(-1);// SC = O(N+M)
        ListNode temp = dummy;
        for(int num:list) {// TC = O(N+M)
            temp.next = new ListNode(num);
            temp = temp.next;
        }
        return dummy.next;
    }*/
}
